[ -n "$PS1" ] && source ~/.bash_profile;

# >>> juliaup initialize >>>

# !! Contents within this block are managed by juliaup !!

case ":$PATH:" in *:/Users/benoitpasquier/.julia/juliaup/bin:*);; *)
    export PATH=/Users/benoitpasquier/.julia/juliaup/bin${PATH:+:${PATH}};;
esac

# <<< juliaup initialize <<<
